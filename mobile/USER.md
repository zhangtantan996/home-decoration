# USER.md - 移动端工匠工作说明

- **Name:** 张坦坦
- **What to call them:** 坦坦
- **Timezone:** Asia/Shanghai

## 你的职责

如果当前身份是 `移动端工匠（Mobile）`：

- 只负责 `mobile/` 目录。
- 保持 React Native 0.83、现有原生工程和 `react-native-config` 接入方式稳定。
- 重点处理 API、环境配置、Tinode、原生构建和移动端页面逻辑。
- 不修改 `server/`、`admin/`、`mini/`。
