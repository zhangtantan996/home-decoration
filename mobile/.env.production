# Production/release profile.
# Inject concrete release values via ENVFILE/CI instead of committing live production endpoints or secrets.
APP_ENV=production
API_BASE_URL=https://api.yourdomain.com
WEB_BASE_URL=https://www.yourdomain.com
TINODE_SERVER_URL=wss://api.yourdomain.com/tinode/v0/channels
TINODE_API_KEY=
