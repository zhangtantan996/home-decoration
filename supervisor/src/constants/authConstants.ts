/**
 * sessionStorage key used by the axios interceptor to persist
 * the reason a forced logout occurred, so the login page can
 * display a contextual "why you were logged out" alert.
 */
export const LOGOUT_REASON_KEY = "supervisor_logout_reason";
