装修设计一体化平台·深度产品设计方案 (PRD)

加载数据慢


docker一键启动
docker-compose -f docker-compose.local.yml build --no-cache api
docker-compose -f docker-compose.local.yml up -d

前端页面  cd "G:\AI_engineering\home_decoration\mobile"
npm run web
管理后台   cd "G:\AI_engineering\home_decoration\admin"
npm run dev
后端API  cd "G:\AI_engineering\home_decoration\server"
go run ./cmd/api
Metro安卓调试  cd "G:\AI engineering\home_decoration\mobile"
npm start   
docker执行  cd "G:\AI engineering\home_decoration"
docker compose up -d db redis

让手机能加载 界面代码。
adb reverse tcp:8081 tcp:8081
让手机能访问 后端数据（API）。
adb reverse tcp:8080 tcp:8080

商家端：http://localhost:5173/merchant/login
管理后台：http://localhost:5173/admin/login


1. 推荐方案：混合极速模式 (适合日常开发)
这个模式下，数据库在后台跑，代码在你本地跑。速度最快，也好调试。
启动命令：
powershell
.\dev_start_robust.ps1
2. 纯 Docker 模式 (适合不装环境)
这个模式下，所有东西都在 Docker 容器里跑。你改代码，Docker 会自动检测到并重启服务。
启动命令：
cd "G:\AI_engineering\home_decoration"
powershell
.\dev_start_docker.ps1

1600120547
56210cab92b337ee8508bb084acc7fd97cb41b22b36256d6f828e09e66509abe

快速参考卡片
操作	命令
查找占用端口的进程	 netstat -ano | findstr :5173
强制关闭进程	    taskkill /F /PID 21696
查看所有 Node 进程	tasklist | findstr node.exe
关闭所有 Node 进程	taskkill /F /IM node.exe
查看进程详情        tasklist | findstr PID号

http://127.0.0.1:8045/v1/messages
sk-7229f6ca79554b40a31bd698230d7267

https://jp.duckcoding.com
sk-3ITVNOgXrTf4P3v5q17wpbWvUKv4NZkYPq6SaBtTVPkfFUqa