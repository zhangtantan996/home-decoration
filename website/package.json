{
    "name": "nestcraft-website",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --port 5175",
        "lint:no-emoji": "node ./scripts/check-no-emoji.mjs",
        "check": "npm run lint:no-emoji && npm run build",
        "build": "vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^6.3.5"
    },
    "dependencies": {
        "lucide": "^0.575.0"
    }
}
