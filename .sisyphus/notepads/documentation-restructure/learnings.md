
### 2026-01-25 部署检查清单补充 (Task 27)
- **内容扩展**: 在 `documentation/07-部署运维/部署指南.md` 中补充了详尽的“部署检查清单”。
- **分类标准**: 将清单划分为“部署前准备”、“部署执行”、“部署后验证”以及“安全与运维专项”四个核心环节，覆盖了从环境搭建到安全加固的全生命周期。
- **安全集成**: 深度集成了 `SECURITY_QUICKSTART.md` 和 `docs/DEPLOYMENT_CHECKLIST.md` 中的安全规范，包括 JWT/ENCRYPTION_KEY 密钥生成、强密码策略、文件权限 (600)、以及生产环境调试端点禁用 (SERVER_MODE=release)。
- **验证机制**: 明确了通过健康检查接口 (`/api/health`)、日志审计及模拟登录等手段进行功能验证。
- **运维最佳实践**: 包含自动重启配置、端口隔离、HTTPS 强制跳转及备份策略等关键运维指标。

### 2026-01-25 Docker 数据持久化文档完善 (Task 28)
- **内容扩展**: 在 `documentation/07-部署运维/Docker配置.md` 中新增了详尽的“数据持久化”章节。
- **持久化策略**: 明确了 PostgreSQL (DB)、Redis (缓存) 和 Uploads (用户上传文件) 的不同持久化需求。
- **环境差异**: 规范了开发环境使用路径映射 (Bind Mounts) 以便调试，而生产环境推荐使用具名卷 (Named Volumes) 以提高稳定性的做法。
- **关键遗漏修复**: 识别并记录了 API 服务中 `./uploads` 目录必须进行持久化映射的要点，防止容器更新导致用户数据丢失。
- **运维建议**: 补充了基于 `pg_dump` 的手动备份与恢复命令示例，并针对生产环境提出了使用云托管数据库 (RDS) 和对象存储 (OSS) 的最佳实践建议。

### 2026-01-25 代码规范文档创建 (Task 31)
- **新文档创建**: 创建了 `documentation/08-开发规范/代码规范.md`，全面覆盖后端 (Go) 与前端 (TypeScript/React) 的开发标准。
- **后端架构标准化**: 明确了 Go 后端必须遵循 `handler -> service -> repository` 的强制分层架构，严禁在 handler 中直接操作数据库。
- **命名规范统一**: 确立了 Go 文件使用 `snake_case`，React 组件使用 `PascalCase`，TS 工具使用 `camelCase` 的跨端统一命名体系。
- **前端质量约束**: 强调了 TypeScript 的严格模式，禁止使用 `any` 类型，优先使用 `import type` 以及 Zustand 状态管理模式。
- **安全与最佳实践**: 集成了日志脱敏、严禁硬编码密钥、以及 Conventional Commits 提交规范，确保代码库的安全与整洁。

### 2026-01-25 Git 工作流文档创建 (Task 32)
- **新文档创建**: 创建了 `documentation/08-开发规范/Git工作流.md`，确立了项目版本管理与团队协作流程。
- **分支策略标准化**: 明确了 `main` (生产) 与 `dev` (开发) 的双主分支架构，并规范了 `feat/`、`fix/`、`hotfix/` 等辅助分支的起源与合并路径。
- **提交规范落地**: 强制执行 Conventional Commits 规范 (feat, fix, docs 等)，并提供了优秀与错误提交消息的对比示例。
- **PR 流程优化**: 建立了 PR 合并前的检查清单（编译、测试、Lint、Review），确保代码合并的质量关卡。
- **版本发布与回滚**: 详细说明了基于语义化版本 (SemVer) 的 Git Tag 打标流程，并针对紧急线上修复 (Hotfix) 提供了标准的操作指令序列。

### 2026-01-25 安全规范文档创建 (Task 33)
- **新文档创建**: 创建了 `documentation/08-开发规范/安全规范.md`，标志着文档重构计划中所有 33 个任务的圆满完成。
- **安全标准集成**: 深度整合了 `docs/SECURITY.md`、`SECURITY_QUICKSTART.md` 及审计报告中的核心准则，涵盖认证授权、数据保护、常见漏洞防护三大领域。
- **认证体系规范**: 明确了 JWT 双 Token 机制（1h Access / 7d Refresh）、密钥环境变量管理、bcrypt 密码哈希以及管理后台 Token 类型强制验证。
- **漏洞防护落地**: 提供了 SQL 注入（GORM 参数化）、XSS（安全响应头）、CSRF（白名单）、限流保护（Redis）及文件上传安全的代码实现参考与检查清单。
- **文档索引闭环**: 更新了 `documentation/README.md`，将开发规范板块的状态统一更新为“已完成”，实现了项目文档体系的标准化。
