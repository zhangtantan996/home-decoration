## 修复记录 - 2026-01-23 继续

### 修复1: 移除 TencentIM 残留代码 ✅
**文件**: `mobile/src/navigation/AppNavigator.tsx`
**修改**: 删除了第190-204行的 TencentIM 初始化和登出代码
**原因**: 用户看到的 `[TencentIM] 初始化成功` 日志来自旧代码，应该只使用 Tinode
**状态**: 已完成

### 修复2: 添加离线消息调试日志 ✅
**文件**: 
- `mobile/src/screens/ChatRoomScreen.tsx`
- `mobile/src/services/TinodeService.ts`

**修改**:
1. 在 `getCachedTopicMessages()` 中添加日志，显示找到的消息数量和第一条消息样本
2. 在 `parseTinodeMessages()` 中添加日志，显示过滤前后的消息数量，并警告如果所有消息被过滤
3. 在 `loadMessages()` 中添加日志，显示订阅过程和历史消息数量
4. 在 `subscribeToConversation()` 中添加详细日志，跟踪整个订阅流程

**目的**: 帮助用户调查为什么离线消息不显示
**状态**: 已完成，等待用户测试并查看日志

### 修复3: 断线重连后重新订阅 me topic ✅
**文件**: `mobile/src/services/TinodeService.ts`
**修改**: 在 `onConnect()` 回调中添加逻辑：
1. 重连成功后重新订阅 me topic
2. 订阅成功后触发 `subs-updated` 事件
3. 添加日志 `[Tinode] ✅ WebSocket 已连接`（用户期望看到的日志）

**原因**: 
- 重连后没有重新订阅 me topic，导致会话列表为空
- 没有触发 subs-updated 事件，MessageScreen 不知道需要刷新

**状态**: 已完成，等待用户测试

