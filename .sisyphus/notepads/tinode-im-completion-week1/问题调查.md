## 问题1调查：离线消息不显示

**时间**: 2026-01-23
**症状**: 会话列表有对话（有时间戳），但点击进入后无聊天记录显示

**代码分析**:

1. **ChatRoomScreen.tsx 的 loadMessages() 流程**:
   - 第227-237行：先尝试从缓存加载消息 (`getCachedTopicMessages`)
   - 第241行：调用 `TinodeService.subscribeToConversation(targetTopic)`
   - 第244-246行：再次获取缓存消息并解析

2. **TinodeService.ts 的 subscribeToConversation() 流程**:
   - 第327行：获取 topic 对象
   - 第330行：构建 getMeta 参数，请求最近50条消息
   - 第351-353行：如果未订阅，先订阅
   - 第354行：调用 `topic.getMeta(getParams)` 拉取消息
   - 第356-360行：等待消息接收完成（最多8秒超时）
   - 第365-375行：等待消息插入缓存

3. **parseTinodeMessages() 过滤逻辑**:
   - 第122行：过滤掉没有 seq 或 seq <= 0 的消息
   - 这可能导致某些消息被过滤掉

**可能的原因**:

1. **消息未正确插入 topic 缓存**:
   - `topic.getMeta()` 可能成功，但消息未插入到 `topic.messages()` 中
   - 需要检查 Tinode SDK 的消息缓存机制

2. **消息被过滤掉**:
   - `parseTinodeMessages` 过滤掉了 seq <= 0 的消息
   - 离线消息可能有特殊的 seq 值

3. **超时时间不够**:
   - 8秒超时可能不够，特别是网络慢的情况

4. **消息格式问题**:
   - 离线消息的格式可能与在线消息不同
   - 需要检查 `msg.content` 的结构

**下一步调查**:
1. 添加详细日志，查看 `getCachedTopicMessages` 返回了什么
2. 检查 `receivedCount` 的值（第337行）
3. 查看 `topic.messageCount()` 的返回值（第372行）
4. 检查消息的 seq 值是否正常


## 问题8调查：iOS 输入框无法弹出键盘

**时间**: 2026-01-23
**症状**: iOS 模拟器中点击输入框无法弹出键盘

**代码分析**:

1. **TextInput 配置** (ChatRoomScreen.tsx 第803-811行):
   ```tsx
   <TextInput
       style={styles.textInput}
       placeholder="输入消息..."
       placeholderTextColor="#A1A1AA"
       value={inputText}
       onChangeText={setInputText}
       multiline
       maxLength={500}
   />
   ```

2. **配置正常**:
   - 有 `value` 和 `onChangeText` 绑定
   - 有 `placeholder`
   - 使用了 `multiline`
   - 没有 `editable={false}` 限制

**结论**:
- 代码配置正确，没有明显问题
- iOS 模拟器的键盘行为与真机不同
- 模拟器可能需要特殊配置才能显示软键盘
- 建议：在 iOS 真机上测试

**建议**:
1. 在 iOS 真机上测试（最可靠）
2. 检查模拟器设置：Hardware > Keyboard > Toggle Software Keyboard
3. 如果真机也有问题，可以尝试添加 `autoFocus={false}` 然后手动控制焦点

**状态**: 代码无问题，可能是模拟器限制

