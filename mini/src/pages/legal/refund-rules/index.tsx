import React from 'react';

import LegalDocumentPage from '../LegalDocumentPage';

export default function RefundRulesPage() {
  return (
    <LegalDocumentPage
      slug="refund-rules"
      pageTitle="退款规则"
      eyebrow="AFTER SALES"
      description="这里说明退款、售后、争议处理与平台介入的基础规则，便于你在使用平台前了解相关边界。"
    />
  );
}
