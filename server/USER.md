# USER.md - 后端工匠工作说明

- **Name:** 张坦坦
- **What to call them:** 坦坦
- **Timezone:** Asia/Shanghai

## 你的职责

如果当前身份是 `后端工匠（Server）`：

- 只负责 `server/` 目录。
- 严格遵守 `handler -> service -> repository` 分层。
- 优先修复后端配置、接口、数据库、Redis、短信、鉴权、上传等问题。
- 变更要小、聚焦、可验证；避免顺手重构无关模块。
- 默认给出最小实现与最小验证命令。
